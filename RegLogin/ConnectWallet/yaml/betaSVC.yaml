apiVersion: v1
kind: Service
metadata:
  namespace: beta
  name: connect-wallet-svc
  annotations:
    networking.gke.io/max-rate-per-endpoint: "30"
spec:
  ports:
  - port: 9004
    targetPort: 8080
  selector:
    POD_LABEL: connect-wallet-pod-label
  type: ClusterIP